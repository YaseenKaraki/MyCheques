# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'My Cheques' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  # Pods for My Cheques
  
  target 'My ChequesTests' do
    inherit! :search_paths
    # Pods for testing
  end
  
  target 'My ChequesUITests' do
    inherit! :search_paths
    # Pods for testing
  end
  
  pod "FSCalendar"
  pod "SkyFloatingLabelTextField"
  pod "Alamofire"
  pod "SwiftyJSON"
  
  
end
